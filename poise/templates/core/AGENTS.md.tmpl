# {{REPO_NAME}}

{{REPO_ONE_LINE_DESCRIPTION}}

---

## Start here

| Question                           | Read                                          |
|------------------------------------|-----------------------------------------------|
| How is this repo structured?       | [docs/architecture.md](docs/architecture.md)  |
| How do I run / test / deploy?      | [docs/workflows.md](docs/workflows.md)        |
| What are the coding conventions?   | [docs/conventions.md](docs/conventions.md)    |
| What is currently being worked on? | [docs/exec-plans/active/](docs/exec-plans/active/) |
| What features are defined?         | [docs/product-specs/](docs/product-specs/)    |
| Why was X decided?                 | [docs/decisions/](docs/decisions/)            |
| Where are external docs / runbooks?| [docs/references/](docs/references/)          |
| What is broken / incomplete?       | [docs/quality.md](docs/quality.md)            |

---

## Layer model (enforced mechanically)

Dependencies must flow strictly forward. No skipping, no reversals.

```
{{LAYER_MODEL_ARROW_DIAGRAM}}
```

Cross-cutting (any layer may import): `{{CROSS_CUTTING_LIST}}`

Violations are caught by `{{CHECK_ARCH_CMD}}` and the post-write hook.
**Error messages include remediation instructions — read and follow them.**

---

## Non-negotiable rules

{{NON_NEGOTIABLE_RULES}}
<!-- One bullet per RESTRICTED_PACKAGE and FORBIDDEN_PACKAGE. Examples:
- No {{RESTRICTED_PACKAGE}} imports outside `{{ALLOWED_LAYER}}/`
- No {{FORBIDDEN_PACKAGE}} anywhere in this repo — use {{ALTERNATIVE}} instead
-->

### Style invariants (enforced by `{{CHECK_STYLE_CMD}}`)

{{STYLE_INVARIANTS_RULES}}
<!-- One bullet per enabled invariant. Examples:
- Files must stay under {{MAX_FILE_LINES}} lines — split along clear seams
- No bare `print()` / `console.log` — use {{LOGGER_HINT}}
- {{NAMING_RULE_SUMMARY}} (see docs/conventions.md#naming)
- {{COMPONENT_RULE_SUMMARY}} (see docs/conventions.md#components)
-->

---

## Commands

```bash
{{EXISTING_COMMANDS}}
{{CHECK_ARCH_CMD}}    # boundary linter — violations include remediation instructions
{{CHECK_STYLE_CMD}}   # golden-principle linter (size, logging, naming, components)
{{GC_CMD}}            # garbage collection: scan docs/code for drift
{{PLAN_CMD}}          # create a new execution plan
```

---

## Plans

Complex work gets an execution plan in `docs/exec-plans/active/`.
Simple tasks (single file, one session): work directly.

**Active plans are created automatically.** When you approve a plan in Claude
Code's plan mode, the `ExitPlanMode` hook writes it to
`docs/exec-plans/active/<slug>.md`. When every checkbox in that plan is marked
`- [x]`, the `Stop` hook moves it to `docs/exec-plans/completed/`. The
`{{PLAN_CMD}}` command is a manual fallback for agents without plan mode.

Completed plans: [docs/exec-plans/completed/](docs/exec-plans/completed/)
